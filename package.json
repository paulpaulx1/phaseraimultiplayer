{
    "name": "phaseraimultiplayer",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
      "start": "node server/server.js",
      "postinstall": "cd client && npm install webpack-cli"
    },
    "engines": {
        "node": ">=18.12.0",
        "npm": ">=7"
    }
  }